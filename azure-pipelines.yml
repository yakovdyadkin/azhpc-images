# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger: none

pool:
  vmImage: ubuntu-latest

steps:

- bash: |
    curl -v -H "Authorization: Bearer $(System.AccessToken)" "https://dev.azure.com/yakovdyadkin/webhook/_apis/build/builds?definitionId=1"
  displayName: Get Most Recent Build 
