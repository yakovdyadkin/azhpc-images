# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger: none

pool:
  vmImage: ubuntu-latest

steps:

- bash: |
    curl -X POST -H "Authorization: Bearer $(System.AccessToken)" -d" {"definition": {"id": 1}" "https://dev.azure.com/yakovdyadkin/webhook/_apis/build/builds?"
  displayName: Queue a Build 
